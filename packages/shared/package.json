{
  "name": "@waiaas/shared",
  "version": "2.16.0",
  "description": "WAIaaS shared constants — pure TypeScript, no native dependencies",
  "license": "MIT",
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/minhoyoo-iotrust/WAIaaS.git",
    "directory": "packages/shared"
  },
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:unit": "vitest run",
    "test:integration": "echo 'no integration tests'",
    "test:security": "echo 'no security tests'",
    "test:chain": "echo 'no chain tests'",
    "test:platform": "echo 'no platform tests'",
    "lint": "echo 'no lint yet'",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf dist"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  }
}
